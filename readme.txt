Bugs:
- /Saving a new city (not via postman) /
- /when first searched, temp doesn't show/

ToDo:
- /render after a city is being removed (click refresh)/
- /prevent from the same city to be saved twice/
- /handle design/
- /turn degrees from F to C /
